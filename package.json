{
  "name": "my-forms",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "generate": "pnpm -r run generate",
    "dev": "concurrently \"pnpm --filter server run start:dev\" \"pnpm --filter client run dev\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.11.0",
  "devDependencies": {
    "concurrently": "^9.2.0"
  }
}
