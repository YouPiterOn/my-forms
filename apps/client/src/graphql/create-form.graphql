mutation CreateForm($input: FormInput!) {
  createForm(input: $input) {
    id
    title
    description
    createdAt
    updatedAt
    questions {
      id
      type
      label
      description
      required
      order
      options
      placeholder
      minDate
      maxDate
    }
  }
}
