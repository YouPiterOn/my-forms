# my-forms

### dev run

#### Prerequisites

- Node 23+
- pnpm (`npm i -g pnpm`)

```bash
# Install all dependencies
pnpm install

# Generate graphql types
pnpm run generate

# Start the development environment
pnpm run dev
```

---

#### run with Docker

```bash
docker compose up
```
